# Pratiksha Bande — Portfolio

Simple static portfolio website. Open `index.html` in a browser or serve from a static server.

Quick preview (from project folder):

```bash
# macOS / Linux
python3 -m http.server 8000

# Windows (PowerShell)
python -m http.server 8000
```

Then open http://localhost:8000 in your browser.
